<UserControl
    x:Class="Test1.ControlsListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Test1"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Test1ViewModel;assembly=Test1ViewModel"
    d:DataContext="{d:DesignInstance vm:Controls}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate x:Key="TestControl">
            <local:TestControlView HorizontalAlignment="Stretch" Style="{DynamicResource UserControlStyle}"/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListBox
            HorizontalContentAlignment="Stretch"
            ItemTemplate="{DynamicResource TestControl}"
            ItemsSource="{Binding ControlsViewModel, Mode=TwoWay}"
            SelectedItem="{Binding SelectedItem}" />
        <Button
            Grid.Row="1"
            HorizontalAlignment="Left"
            Command="{Binding AddControlCommand}"
            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
            <Image Height="20" Source="Resources/add.png" />
        </Button>
    </Grid>
</UserControl>
