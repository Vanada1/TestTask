<UserControl
    x:Class="Test1.TestControlView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="clr-namespace:Test1ViewModel;assembly=Test1ViewModel"
    xmlns:local="clr-namespace:Test1"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance vm:TestControlViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid x:Name="TaskGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" 
                   Text="{Binding FileName}"
                   Margin="3"/>
        <Button
            Grid.Column="1"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
            Command="{Binding RemoveCommand, ConverterParameter=this}" 
            Margin="3">
            <Image Height="18" Source="Resources/remove.png" />
            <Button.CommandParameter>
                <Binding />
            </Button.CommandParameter>
        </Button>
    </Grid>
</UserControl>
